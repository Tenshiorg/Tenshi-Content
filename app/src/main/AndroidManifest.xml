<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.shadow578.tenshicontent">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TenshiContent">
        <activity android:name=".TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 4anime (jsoup) -->
        <service
            android:name=".fouranime.jsoup.FourAnimeSoupAdapterService"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.shadow578.tenshi.content.ADAPTER" />

                <category android:name="io.github.shadow578.tenshi.content.ADAPTER" />
            </intent-filter>

            <meta-data
                android:name="io.github.shadow578.tenshi.content.DISPLAY_NAME"
                android:value="@string/fouranime_display_name_soup" />
            <meta-data
                android:name="io.github.shadow578.tenshi.content.ADAPTER_VERSION"
                android:value="1" />
            <meta-data
                android:name="io.github.shadow578.tenshi.content.UNIQUE_NAME"
                android:value="io.github.shadow578.tenshicontent.fouranime.soup" />
        </service>

        <!-- 4anime (webview) -->
        <service
            android:name=".fouranime.webview.FourAnimeWebViewAdapterService"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.shadow578.tenshi.content.ADAPTER" />

                <category android:name="io.github.shadow578.tenshi.content.ADAPTER" />
            </intent-filter>

            <meta-data
                android:name="io.github.shadow578.tenshi.content.DISPLAY_NAME"
                android:value="@string/fouranime_display_name_webview" />
            <meta-data
                android:name="io.github.shadow578.tenshi.content.ADAPTER_VERSION"
                android:value="1" />
            <meta-data
                android:name="io.github.shadow578.tenshi.content.UNIQUE_NAME"
                android:value="io.github.shadow578.tenshicontent.fouranime.webview" />
        </service>

        <activity android:name=".fouranime.webview.FourAnimeWebViewActivity"
            android:label="@string/fouranime_display_name_webview"/>
    </application>

</manifest>